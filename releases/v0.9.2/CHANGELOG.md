# ObtuseLoot v0.9.2 Changelog

- Formalized deterministic artifact seed lifecycle (`artifact-seed`, reseed/recreate flows).
- Expanded debug command coverage including `simulate` and `seed` branches.
- Added repository-level simulation harnesses and analytics output structure.
- Added multi-run validation and confidence scoring documents for balance review.
