package com.falcoignis.obtuseloot.engine;

final class EvolutionEngine implements EngineSubsystem {
    private final ObtuseEngine engine;

    EvolutionEngine(ObtuseEngine engine) {
        this.engine = engine;
    }
}
