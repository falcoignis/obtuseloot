package com.falcoignis.obtuseloot.engine;

final class ArtifactProcessor implements EngineSubsystem {
    private final ObtuseEngine engine;

    ArtifactProcessor(ObtuseEngine engine) {
        this.engine = engine;
    }
}
