package com.falcoignis.obtuseloot.engine;

final class ReputationEngine implements EngineSubsystem {
    private final ObtuseEngine engine;

    ReputationEngine(ObtuseEngine engine) {
        this.engine = engine;
    }
}
