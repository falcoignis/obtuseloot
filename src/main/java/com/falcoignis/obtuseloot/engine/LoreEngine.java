package com.falcoignis.obtuseloot.engine;

final class LoreEngine implements EngineSubsystem {
    private final ObtuseEngine engine;

    LoreEngine(ObtuseEngine engine) {
        this.engine = engine;
    }
}
