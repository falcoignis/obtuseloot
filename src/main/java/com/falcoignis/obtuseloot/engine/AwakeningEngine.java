package com.falcoignis.obtuseloot.engine;

final class AwakeningEngine implements EngineSubsystem {
    private final ObtuseEngine engine;

    AwakeningEngine(ObtuseEngine engine) {
        this.engine = engine;
    }
}
