# ObtuseLoot v0.9.3

Release metadata refresh for the v0.9.3 testing cut.

## Included artifact
- `ObtuseLoot-0.9.3.jar`

## Build command
- `mvn -B -ntp clean package`

## Notes
- Version is aligned in `pom.xml` and `plugin.yml`.
- Release records are available under `releases/v0.9.3/`.
