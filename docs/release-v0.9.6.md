# ObtuseLoot v0.9.6

Release metadata refresh for the v0.9.6 build.

## Included artifact
- `ObtuseLoot-0.9.6.jar`

## Build command
- `mvn -B -ntp clean package`

## Notes
- Version is aligned in `pom.xml` and `plugin.yml` (`0.9.6`).
- Release records are available under `releases/v0.9.6/`.
- Tag pushes matching `v*` run `.github/workflows/release.yml` to build and publish release assets.
